package com.wiom.csp.ui.theme

import androidx.compose.ui.graphics.Color

/** Dark theme colors (default) -- ported from globals.css :root */
object DarkColors {
    // Brand
    val brandPrimary = Color(0xFFD9008D)
    val brandLight = Color(0xFFFFB2E4)
    val brandLighter = Color(0xFFFFCCED)
    val brandLightest = Color(0xFFFFE5F6)
    val brandSecondary = Color(0xFF443152)

    // Gold Gradient
    val goldStart = Color(0xFFD5AF4A)
    val goldMid = Color(0xFFF2E983)
    val goldEnd = Color(0xFFD5B14B)

    // Backgrounds
    val bgPrimary = Color(0xFF161021)
    val bgSecondary = Color(0xFF352D42)
    val bgCard = Color(0xFF443152)
    val bgCardHover = Color(0xFF665E75)

    // Text
    val textPrimary = Color(0xFFFAF9FC)
    val textSecondary = Color(0xFFA7A1B2)
    val textMuted = Color(0xFF665E75)

    // Semantic
    val positive = Color(0xFF008043)
    val positiveLight = Color(0xFFA5E5C6)
    val positiveBg = Color(0xFFE1FAED)
    val negative = Color(0xFFD92130)
    val negativeLight = Color(0xFFFFB0B8)
    val negativeBg = Color(0xFFFFE5E7)
    val warning = Color(0xFFFF8000)
    val warningDark = Color(0xFFB85C00)
    val warningLight = Color(0xFFFFAA00)
    val warningBg = Color(0xFFFFE6CC)

    // Borders
    val borderSubtle = Color(0xFF352D42)
    val cardBorder = Color(0xFF2A2D3A)

    // Chip
    val chipBg = Color(0xFF352D42)

    // Money
    val money = Color(0xFF2196F3)
    val moneyLight = Color(0xFF90CAF9)
    val moneyBg = Color(0x1F2196F3)

    // Task accents
    val accentRestore = Color(0xFF6D17CE)
    val accentGold = Color(0xFFD5AF4A)

    // Strip
    val stripBg = Color(0xFF161021)

    // Subtle backgrounds
    val positiveSubtle = Color(0x1A008043)
    val negativeSubtle = Color(0x14D92130)
    val warningSubtle = Color(0x1AFF8000)
    val brandSubtle = Color(0x14D9008D)
    val moneySubtle = Color(0x142196F3)
    val restoreSubtle = Color(0x266D17CE)
    val goldSubtle = Color(0x26D5AF4A)

    // Card gradient
    val cardGradientStart = Color(0xFF443152)
    val cardGradientEnd = Color(0xFF665E75)

    // Info / Caution
    val info = Color(0xFF6D17CE)
    val infoBg = Color(0x26F1E5FF)
    val infoSubtle = Color(0x266D17CE)
    val caution = Color(0xFF806000)
    val cautionBg = Color(0x26FFF2BF)
    val cautionSubtle = Color(0x26806000)

    // CTA Disabled
    val ctaDisabledBg = Color(0xFFA7A1B2)

    // Overlay
    val overlayBg = Color(0x99000000)
}

/** Light theme — aligned to Wiom Design System tokens */
object LightColors {
    // Brand
    val brandPrimary = Color(0xFFD9008D)
    val brandLight = Color(0xFFFFB2E4)
    val brandLighter = Color(0xFFFFCCED)
    val brandLightest = Color(0xFFFFE5F6)
    val brandSecondary = Color(0xFFE8E4F0)

    // Gold Gradient
    val goldStart = Color(0xFFD5AF4A)
    val goldMid = Color(0xFFF2E983)
    val goldEnd = Color(0xFFD5B14B)

    // Backgrounds
    val bgPrimary = Color(0xFFFAF9FC)
    val bgSecondary = Color(0xFFE8E4F0)
    val bgCard = Color(0xFFFFFFFF)
    val bgCardHover = Color(0xFFF1EDF7)

    // Text
    val textPrimary = Color(0xFF161021)
    val textSecondary = Color(0xFF665E75)
    val textMuted = Color(0xFFA7A1B2)

    // Semantic
    val positive = Color(0xFF008043)
    val positiveLight = Color(0xFFA5E5C6)
    val positiveBg = Color(0xFFE1FAED)
    val negative = Color(0xFFD92130)
    val negativeLight = Color(0xFFFFB0B8)
    val negativeBg = Color(0xFFFFE5E7)
    val warning = Color(0xFFFF8000)
    val warningDark = Color(0xFFB85C00)
    val warningLight = Color(0xFFFFAA00)
    val warningBg = Color(0xFFFFE6CC)

    // Borders
    val borderSubtle = Color(0xFFE8E4F0)
    val cardBorder = Color(0xFFE8E4F0)

    // Chip
    val chipBg = Color(0xFFE8E4F0)

    // Money
    val money = Color(0xFF2196F3)
    val moneyLight = Color(0xFF90CAF9)
    val moneyBg = Color(0x1F2196F3)

    // Task accents
    val accentRestore = Color(0xFF6D17CE)
    val accentGold = Color(0xFFD5AF4A)

    // Strip
    val stripBg = Color(0xFFE8E4F0)

    // Subtle backgrounds
    val positiveSubtle = Color(0x0F008043)
    val negativeSubtle = Color(0x0FD92130)
    val warningSubtle = Color(0x0FFF8000)
    val brandSubtle = Color(0x0FD9008D)
    val moneySubtle = Color(0x0F2196F3)
    val restoreSubtle = Color(0x146D17CE)
    val goldSubtle = Color(0x14D5AF4A)

    // Info / Caution
    val info = Color(0xFF6D17CE)
    val infoBg = Color(0xFFF1E5FF)
    val infoSubtle = Color(0x0F6D17CE)
    val caution = Color(0xFF806000)
    val cautionBg = Color(0xFFFFF2BF)
    val cautionSubtle = Color(0x0F806000)

    // CTA Disabled
    val ctaDisabledBg = Color(0xFFA7A1B2)

    // Card gradient
    val cardGradientStart = Color(0xFFFFFFFF)
    val cardGradientEnd = Color(0xFFF1EDF7)

    // Overlay
    val overlayBg = Color(0x4D000000)
}
